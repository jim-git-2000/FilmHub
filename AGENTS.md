# 工作区规则

## 第一性原理

所有决策从问题本质出发，不因「惯例如此」照搬。回到问题本身：要解决什么？最直接的路径是什么？从零设计会怎么做？

不要谄媚。不要夸我的想法好、不要说「这是个很好的问题」、不要开头加「当然可以」。给我真实判断——方案有问题直接指出来。发现更好的做法直接说，不用等我问。

## 约束先行

无论开发项目还是知识管理项目，第一步永远是建规则：新项目先写 `AGENTS.md`，新目录先定结构约定（什么放哪、怎么命名、何时清理）。没有规范的工作空间不动手。

已有规范的项目，严格遵守其 `AGENTS.md` 中的约定。需要调整规范时先改文档、再改实践，不要反过来。

## 交互设计原则

**用户体验是所有产品的最高准则，优先级高于技术偏好、代码整洁度、架构优雅度。后端可以很复杂，但用户触碰到的每一层必须丝滑。**

这不只是 GUI——CLI、对话式交互、Skill、系统反馈，都是交互体验。所有界面都适用以下原则：

- **为目标设计，不为功能设计**：先问「用户要完成什么」，再决定怎么实现。不要因为技术上能做就加功能
- **不要让用户思考**：交互应该不言自明。需要说明书才能用，设计就是失败的
- **系统承担复杂性**：能自动化的不手动，能推断的不让用户填，能一步完成的不拆成三步
- **渐进式展示**：先给核心，细节按需展开。不要一次性把所有选项甩给用户
- **反馈引导行动**：不要只报告问题（「连接失败」），要引导下一步（「正在重试，预计 5 秒后恢复」）

## 工作方式

- 默认中文，代码、命令、变量名用英文
- 结论先行，再给理由，不要先铺垫背景
- 遇到模糊需求，先给最合理的方案，再问要不要调整
- 不要问「你确定要这样吗」——除非有真实风险

## 开发习惯

- `README.md` 和面向用户的项目文档默认使用中文；代码、命令、配置键和变量名保持英文
- 开发工作在本地工作区完成，只使用已经具备的工具做编辑和轻量检查；除非用户明确要求，不安装或升级 SDK、workload、IDE、全局工具和其他开发环境
- 尽量减少依赖和环境配置：优先使用标准库，提供可直接使用的默认值与配置模板，不把本机环境变量或手工配置当作正常使用前提
- 依赖还原、正式 build、test、publish 和打包默认由 GitHub Actions 自动完成；项目有专用 CI 配置时，本地检查不得绕过或改写该配置
- 本机环境无法执行某项正式验证时必须明确报告，不得为验证临时安装环境，并以 GitHub Actions 对应平台的构建结果作为正式依据
- GitHub Actions 应产出可直接下载验证的 artifact 或 Release；由用户手动下载并在目标环境验收，人工验证结果是发布依据，不自动部署
- 改完主动跑验证（test / lint / build），不要只改不验
- 不要为了让代码跑起来而注释掉报错，找根本原因
- 密钥、token、密码不进代码

## Git 与部署

- commit message 用中文，简洁描述变更意图
- `git push` 仅用于跨设备同步，不要自动执行，等我说
- 部署走项目自己的命令（查项目 `AGENTS.md`），不依赖 `git push`

## FilmHub 目录与实现约定

- `docs/PLAN_v1.md` 是产品计划（文内版本为 v2）；实现进度和验收限制记录在 `docs/IMPLEMENTATION.md`，部署和备份说明放在中文 `README.md`。
- `frontend/` 使用 Next.js App Router、TypeScript 和原生 CSS；`src/app/` 放路由，`src/components/` 按 film、roll、photo、library、layout 分组，`src/lib/` 放 API 客户端，`src/types/` 放共享类型。测试放 `frontend/tests/`。
- `backend/app/` 使用 FastAPI；`models/` 放 SQLite 表结构，`schemas/` 放输入校验，`routers/` 放 HTTP 适配，`services/` 放业务、图片及备份服务。SQLite 使用 Python 标准库，不增加 ORM。测试放 `backend/tests/`。
- `docker/` 放容器构建文件；根目录 `compose.yaml` 是唯一部署入口，运行 `docker compose up -d`。默认只绑定本机端口，通过浏览器访问；不自动部署。
- `.github/workflows/` 放正式 CI：后端集成测试、前端类型检查和浏览器测试、容器构建及可下载镜像 artifact；GHCR 发布只允许手动触发。
- `scripts/` 仅放标准库轻量检查；不在本机还原依赖或安装环境。已有 Python/Pillow 可用于轻量服务验证，正式测试仍以 CI 为准。
- 运行数据放 `/data`、`/uploads`、`/backups` 三个持久卷，本地默认放被忽略的 `var/`；上传原图按胶卷编号进入 `rolls/000036/originals/`，缩略图进入 `thumbnails/`。临时上传与恢复文件由服务清理，不提交生成文件、运行数据、依赖目录或密钥。
- 所有写入经同一进程锁协调；后端固定单 worker，保证数据库、上传和备份恢复的一致性。器材被引用时拒绝删除；收藏筛选指至少有一张收藏照片的胶卷。
- 草稿只保存在当前浏览器，正式胶卷必须有胶片和相机。生命周期自动前进，已归档卷不会因补录或上传退回。
